---
import cerdo from "@/assets/cerdo.webp";
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <div
    class="relative w-full max-w-[1800px] max-h-[1930px] mx-auto h-full px-8 flex items-start py-20 border-2 border-green-400 ">
    <div class="relative w-full  border-2 border-red-400">
      {/* Imagen Principal */}
      <img
        src={cerdo.src}
        alt="Imagen Principal"
        class="w-[80%] h-auto relative z-10 bg-gray-100"
      />

      {/* Imagen Secundaria */}
      <div
        class="absolute w-[80%] h-auto max-h-[470px] -bottom-[20%] xl:-bottom-[35%] right-0 2xl:right-10 z-20 shadow-lg border-2 border-blue-400 flex">
        <div class="flex flex-col w-full justify-end  gap-4">
            <h1 class="text-6xl font-custom2">COBERTURA</h1>
            <hr class="w-full border border-gray-700" />
            <p class="text-2xl font-custom2 text-gray-700">
              ¿Qué es?
            </p>
            <p class="text-gray-700 max-w-md">
                La cobertura es un servicio que nos permite asistir a nuestros
                residentes en casa, con el fin de que puedan disfrutar de la
                atención médica que nos brinda. 
            </p>
        </div>
        <img
          src={cerdo.src}
          alt="Imagen Secundaria"
          class="md:w-[500px] xl:max-w-[700px] xl:w-[50%] object-cover"
        />
      </div>
    </div>
  </div>
</Layout>
